# Namecheap DynDNS bash

## Usage

```
bash namecheap-dyndns.sh domain_name ddns_password
```

## Or with **data.txt** in the file's directory

```
bash start.sh
```

The **data.txt** needs to contain two lines:
```
domain_name
ddn_password
```